<Page
    x:Class="ClassManager.Views.ActivityPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClassManager.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:ClassManager.Models"
    mc:Ignorable="d">

    <Page.Resources>
        
        <DataTemplate x:Key="ActivityItemsTemplate" x:DataType="data:Activity">
            <StackPanel Orientation="Horizontal"
                        Margin="10"
                        Background="Yellow">
                <controls:ImageEx Source="{x:Bind Thumbnail, Mode=OneWay}"
                                  Height="100" Width="100"
                                  Stretch="UniformToFill"/>
                <TextBlock Text="{x:Bind Name, Mode=OneWay}"
                           TextWrapping="Wrap"
                           Width="100"/>
            </StackPanel>
        </DataTemplate>
        
    </Page.Resources>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>


        <controls:AdaptiveGridView x:Name="ActivityAdaptiveGridView"
                                   ItemsSource="{x:Bind vm.Activities, Mode=OneWay}"
                                   ItemTemplate="{StaticResource ActivityItemsTemplate}"
                                   Grid.Column="0" Grid.Row="0"
                                   DesiredWidth="120" >
            
        </controls:AdaptiveGridView>
    </Grid>
</Page>
